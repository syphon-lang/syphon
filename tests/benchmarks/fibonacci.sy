benchmarker = import("benchmarker.sy")

benchmarker.run(fn () {
    fib = fn (n) {
        if n < 2 {
            return n
        }

        return fib(n - 1) + fib(n - 2)
    }

    println(fib(30))
})
