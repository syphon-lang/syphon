tester = import("tester.sy")

test_1 = fn () {
    # sleep for one second
    time = import("time")

    start = time.now()
    time.sleep(1)
    end = time.now()

    return end - start == 1
}

tester.test([
    test_1,
])
